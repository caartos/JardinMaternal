import { StyleSheet } from "react-native";

const dateTimePicker = StyleSheet.create({
  dateTimePicker: {
    backgroundColor: "white",
    marginTop: 10,
    borderRadius: 50,
    width: "85%",
    height: 55,
    paddingLeft: 10,
  },
});

export default dateTimePicker