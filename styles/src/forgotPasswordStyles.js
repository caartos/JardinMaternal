import { StyleSheet } from "react-native";

const forgotPasswordStyles = StyleSheet.create({
  forgotPasswordTextTag: {
    marginTop: 20,
    marginBottom: 30,
    color: "#001f8b",
  },
  forgotPasswordMainViewTag: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});

export default forgotPasswordStyles;
